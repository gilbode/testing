version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world on branch 2.
      - run:
          name: Print the Current Time
          command: date
workflows:
   version: 2
   nightly:
     triggers:
       - schedule:
           cron: "*/12 0 * * *"
     jobs:
       - build

