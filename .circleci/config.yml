version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world on branch 2.
      - run:
          name: Print the Current Time
          command: date
workflows:
   version: 2
   nightly:
     triggers:
       - schedule:
           cron: "0, 5,10,15,20,25,30,35,40,45,50,55 * * * *"
     jobs:
       - build

